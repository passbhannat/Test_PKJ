CREATE PROC PROC_ACCOUNT_GLCODE
AS
SELECT "AcctCode" FROM OACT 
WHERE "Finanse" = 'Y'
GO


alter PROC PROC_ACCOUNT_PREVIOUS_BALANCE
@AcctCode Varchar(50)
AS
SELECT SUM("U_RecoBal")
FROM "@BANKRECO" T0 
WHERE T0."U_BankGL" = @AcctCode;
GO